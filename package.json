{
  "name": "bagipangan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:fe": "npm run dev --prefix fe-nextjs",
    "dev:be": "cd be-laravel && php artisan serve",
    "dev": "npx concurrently -n \"frontend,backend\" -c \"cyan,magenta\" \"npm:dev:fe\" \"npm:dev:be\"",
    "test": "npm run test:tc",
    "test:tc": "node --test tests/automated/*.test.mjs",
    "test:be": "cd be-laravel && php artisan test",
    "test:fe": "npm run lint --prefix fe-nextjs && npm run build --prefix fe-nextjs",
    "test:ci": "npm run test:tc && npm run test:be && npm run test:fe"
  },
  "dependencies": {
    "react-icons": "^5.6.0",
    "sharp": "^0.34.5"
  }
}
